# Инструкция по контейнеризации проекта с Docker

## 📦 Что было сделано

1. ✅ **Улучшен Dockerfile** - оптимизирован для работы с проектом
2. ✅ **Создан docker-compose.yml** - для удобного управления контейнером
3. ✅ **Обновлен .dockerignore** - исключены ненужные файлы из образа

## 🚀 Быстрый старт

### Вариант 1: Использование Docker Compose (рекомендуется)

1. **Убедитесь, что у вас есть файл `.env`** с необходимыми переменными окружения:
```env
BOT_TOKEN=your_telegram_bot_token
DEEPSEEK_API_KEY=your_deepseek_api_key
# ... остальные переменные
```

2. **Соберите и запустите контейнер:**
```bash
docker-compose up -d
```

3. **Просмотр логов:**
```bash
docker-compose logs -f
```

4. **Остановка контейнера:**
```bash
docker-compose down
```

### Вариант 2: Использование Docker напрямую

1. **Соберите образ:**
```bash
docker build -t abricol-assistant .
```

2. **Запустите контейнер:**
```bash
docker run -d \
  --name abricol-bot \
  --env-file .env \
  -v $(pwd)/abricol.db:/app/abricol.db \
  -v $(pwd)/knowledge.db:/app/knowledge.db \
  -v $(pwd)/leads.xlsx:/app/leads.xlsx \
  -v $(pwd)/src/knowledge/data:/app/src/knowledge/data \
  -v $(pwd)/bot.log:/app/bot.log \
  abricol-assistant
```

3. **Просмотр логов:**
```bash
docker logs -f abricol-bot
```

4. **Остановка контейнера:**
```bash
docker stop abricol-bot
docker rm abricol-bot
```

## 📋 Подробные инструкции

### Шаг 1: Подготовка файла .env

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# Telegram Bot
BOT_TOKEN=your_telegram_bot_token
ADMIN_CHAT_ID=your_admin_chat_id

# Database
DB_PATH=sqlite+aiosqlite:///./abricol.db

# DeepSeek API
DEEPSEEK_API_KEY=your_deepseek_api_key
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_BASE_URL=https://api.deepseek.com

# Speech-to-Text
STT_MODEL_SIZE=medium
STT_DEVICE=cpu
STT_COMPUTE_TYPE=int8
STT_LANGUAGE=ru

# Excel export
LEADS_EXCEL_PATH=./leads.xlsx

# Google Sheets (опционально)
GOOGLE_SHEETS_CREDENTIALS_PATH=./path/to/credentials.json
GOOGLE_SHEETS_SPREADSHEET_ID=your_spreadsheet_id
```

### Шаг 2: Сборка образа

```bash
# С использованием docker-compose
docker-compose build

# Или напрямую с Docker
docker build -t abricol-assistant .
```

### Шаг 3: Запуск контейнера

#### С docker-compose:
```bash
# Запуск в фоновом режиме
docker-compose up -d

# Запуск с просмотром логов
docker-compose up
```

#### С Docker напрямую:
```bash
docker run -d \
  --name abricol-bot \
  --restart unless-stopped \
  --env-file .env \
  -v $(pwd)/abricol.db:/app/abricol.db \
  -v $(pwd)/knowledge.db:/app/knowledge.db \
  -v $(pwd)/leads.xlsx:/app/leads.xlsx \
  -v $(pwd)/src/knowledge/data:/app/src/knowledge/data \
  abricol-assistant
```

### Шаг 4: Проверка работы

```bash
# Просмотр логов
docker-compose logs -f

# Или для прямого Docker
docker logs -f abricol-bot

# Проверка статуса контейнера
docker-compose ps

# Или
docker ps | grep abricol
```

## 🔧 Управление контейнером

### Остановка
```bash
docker-compose stop
# или
docker stop abricol-bot
```

### Запуск после остановки
```bash
docker-compose start
# или
docker start abricol-bot
```

### Перезапуск
```bash
docker-compose restart
# или
docker restart abricol-bot
```

### Удаление контейнера
```bash
docker-compose down
# или
docker stop abricol-bot && docker rm abricol-bot
```

### Пересборка образа

#### Вариант 1: Использование скрипта (рекомендуется для Windows)
```powershell
.\rebuild_docker.ps1
```

Скрипт автоматически:
1. Остановит и удалит существующий контейнер
2. Опционально удалит старый образ
3. Пересоберет образ без кэша
4. Запустит новый контейнер
5. Покажет статус

#### Вариант 2: Ручная пересборка
```bash
# 1. Остановить и удалить контейнер
docker-compose down

# 2. Пересобрать образ без кэша
docker-compose build --no-cache

# 3. Запустить новый контейнер
docker-compose up -d

# 4. Проверить логи
docker-compose logs -f
```

#### Вариант 3: Пересборка с удалением старого образа
```bash
# 1. Остановить и удалить контейнер
docker-compose down

# 2. Удалить старый образ (опционально)
docker rmi $(docker-compose config --images) -f

# 3. Пересобрать образ
docker-compose build --no-cache

# 4. Запустить контейнер
docker-compose up -d
```

## 📁 Монтирование томов

В `docker-compose.yml` настроены следующие тома:

- `./abricol.db` → `/app/abricol.db` - основная база данных
- `./knowledge.db` → `/app/knowledge.db` - база знаний
- `./leads.xlsx` → `/app/leads.xlsx` - Excel файл с лидами
- `./src/knowledge/data` → `/app/src/knowledge/data` - данные базы знаний
- `./bot.log` → `/app/bot.log` - логи бота

**Важно:** Эти файлы будут сохраняться на хосте даже после удаления контейнера.

## 🔄 Обновление бота

### Быстрый способ (Windows PowerShell):
```powershell
.\rebuild_docker.ps1
```

### Ручной способ:

1. **Остановите контейнер:**
```bash
docker-compose down
```

2. **Обновите код** (через git или вручную)

3. **Пересоберите образ:**
```bash
docker-compose build --no-cache
```

4. **Запустите снова:**
```bash
docker-compose up -d
```

5. **Проверьте логи:**
```bash
docker-compose logs -f
```

## 🐛 Решение проблем

### Проблема: Контейнер не запускается

**Решение:**
```bash
# Проверьте логи
docker-compose logs

# Проверьте, что .env файл существует и содержит все необходимые переменные
cat .env
```

### Проблема: База знаний не работает

**Решение:**
```bash
# Войдите в контейнер
docker-compose exec abricol-bot bash

# Соберите базу знаний вручную
python -m src.build_kb

# Выйдите из контейнера
exit
```

### Проблема: Файлы не сохраняются

**Решение:**
- Убедитесь, что пути к томам в `docker-compose.yml` правильные
- Проверьте права доступа к файлам на хосте
- Убедитесь, что файлы не заблокированы другими процессами

### Проблема: Ошибка при сборке образа

**Решение:**
```bash
# Очистите кэш Docker
docker system prune -a

# Пересоберите образ
docker-compose build --no-cache
```

## 📊 Мониторинг

### Просмотр использования ресурсов
```bash
docker stats abricol-bot
```

### Просмотр логов в реальном времени
```bash
docker-compose logs -f --tail=100
```

### Выполнение команд внутри контейнера
```bash
docker-compose exec abricol-bot bash
```

## 🔒 Безопасность

1. **Не коммитьте `.env` файл в Git** - он уже в `.gitignore`
2. **Используйте секреты Docker** для продакшена:
```bash
docker secret create bot_token your_token
```

3. **Ограничьте ресурсы** через `docker-compose.yml` (уже настроено)

## 📝 Примечания

- При первом запуске база знаний может собираться автоматически (если раскомментировать строку в Dockerfile)
- Excel файлы могут быть заблокированы, если открыты в Excel - бот автоматически сохранит во временный файл
- Логи сохраняются в `bot.log` на хосте

## 🚀 Продакшн

Для продакшн-развертывания рекомендуется:

1. Использовать Docker Compose с ограничениями ресурсов
2. Настроить автоматический перезапуск (`restart: unless-stopped`)
3. Настроить ротацию логов
4. Использовать Docker secrets для чувствительных данных
5. Настроить мониторинг (например, через Prometheus)

