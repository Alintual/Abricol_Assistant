# Abricol_Assistant | ШБ Абриколь - Консультант https://t.me/Abricol_bot
Всё о школе бильярда "Абриколь" и русском бильярде - 
✅консультации ✅запись ✅справочник

Telegram-бот для школы русского бильярда «Абриколь» — интеллектуальный консультант с поддержкой AI, голосового ввода и автоматизированной системой записи на обучение.

## 📋 Описание проекта

**Abricol Assistant** — это комплексный Telegram-бот, разработанный для автоматизации работы школы бильярда. Бот предоставляет консультации по правилам игры, обучению и техническим вопросам, обрабатывает заявки на обучение и консультации, а также ведет учет клиентов.

### Основные возможности

- 🤖 **AI-консультант** на базе DeepSeek для ответов на вопросы о бильярде
- 📚 **База знаний** из PDF-документов с полнотекстовым поиском
- 🎤 **Голосовой ввод** с распознаванием речи (Whisper)
- 📝 **Система записи** на обучение и консультации
- 📊 **Учет клиентов** с экспортом в Excel и Google Sheets
- 🔒 **Политика конфиденциальности** с согласием пользователей
- 💬 **История диалогов** с сохранением всех сообщений

## 🏗️ Архитектура проекта

### Технологический стек

- **Python 3.11** — основной язык разработки
- **aiogram 3.4.1** — фреймворк для Telegram Bot API
- **SQLAlchemy 2.0** — ORM для работы с базой данных
- **SQLite** — база данных (с поддержкой aiosqlite)
- **DeepSeek API** — AI-модель для генерации ответов
- **faster-whisper** — распознавание речи
- **openpyxl** — работа с Excel файлами
- **pypdf** — обработка PDF документов
- **Docker** — контейнеризация для быстрого развертывания

### Структура проекта

```
Abricol_Assistant/
├── src/
│   ├── bot.py                 # Точка входа, запуск бота
│   ├── config.py              # Конфигурация и настройки
│   ├── build_kb.py            # Сборка базы знаний из PDF
│   ├── deepseek_client.py      # Клиент для DeepSeek API
│   ├── stt_client.py          # Клиент для распознавания речи
│   ├── handlers/              # Обработчики сообщений
│   │   ├── start.py            # Команда /start и главное меню
│   │   ├── faq.py              # Обработка вопросов и FAQ
│   │   ├── booking.py          # Запись на обучение
│   │   └── policy.py           # Политика конфиденциальности
│   ├── db/                     # Работа с базой данных
│   │   ├── models.py           # Модели данных (SQLAlchemy)
│   │   ├── session.py          # Настройка сессий БД
│   │   ├── user_profile.py     # Профили пользователей
│   │   ├── chat_history.py     # История сообщений
│   │   ├── contact_journal.py # Журнал обращений
│   │   └── leads_excel.py     # Экспорт в Excel
│   └── knowledge/              # База знаний
│       ├── data/               # PDF документы и изображения
│       ├── text_search.py      # Полнотекстовый поиск
│       └── image_mapper.py     # Привязка изображений
├── requirements.txt            # Зависимости Python
├── Dockerfile                  # Конфигурация Docker для контейнеризации
├── docker-compose.yml          # Docker Compose для удобного управления
├── .dockerignore              # Исключения для Docker
├── DOCKER.md                   # Подробная документация по Docker
└── README.md                   # Документация
```

## 🚀 Функционал

### 1. Консультации и FAQ

- **Текстовые вопросы** — пользователь может задавать вопросы текстом
- **Голосовые сообщения** — поддержка голосового ввода с автоматическим распознаванием
- **Поиск по базе знаний** — полнотекстовый поиск по PDF документам
- **AI-ответы** — генерация ответов с использованием DeepSeek API
- **Ссылки на первоисточники** — автоматическое определение и предоставление ссылок на PDF документы
- **Изображения** — показ релевантных изображений из документов

### 2. Система записи

Бот поддерживает два типа записей:

#### Запись на обучение
1. **Фаза 1**: Выбор статуса (Обучение/Консультация)
2. **Фаза 2**: Согласие с политикой конфиденциальности
3. **Фаза 3**: Анкетирование
   - Опыт игры
   - Уровень подготовки
   - Цели обучения
   - Обучался ли ранее в «Абриколь»
4. **Фаза 4**: Запись контактов
   - Имя
   - Телефон

#### Запись на консультацию
- Упрощенный процесс без анкетирования
- Только сбор контактных данных

### 3. Учет и аналитика

- **База данных SQLite** — хранение всех данных о пользователях
- **Экспорт в Excel** — автоматическое сохранение лидов в `leads.xlsx`
- **Google Sheets** — интеграция с Google Sheets для журнала обращений
- **История диалогов** — полное сохранение всех сообщений
- **Профили пользователей** — отслеживание статуса и данных клиентов

### 4. База знаний

База знаний формируется из PDF документов:

- Правила игры Корона (2021)
- Международные правила Пирамиды (2018)
- Технические требования к бильярдным столам ФБСР (2020)
- Общая информация о школе
- Виды обучения
- Методика обучения
- Система тренировок БИСА

**Процесс обработки:**
1. Извлечение текста из PDF
2. Извлечение и сохранение изображений
3. Структурирование текста
4. Построение индекса полнотекстового поиска

## 📦 Установка и запуск

### Требования

**Для локального запуска:**
- Python 3.11+
- pip

**Для Docker-развертывания:**
- Docker и Docker Compose
- Файл `.env` с настройками

### Локальная установка

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd Abricol_Assistant
```

2. **Создайте виртуальное окружение:**
```bash
# Windows (PowerShell)
python -m venv .venv
.\.venv\Scripts\Activate.ps1

# Linux/Mac
python3 -m venv .venv
source .venv/bin/activate
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Настройте переменные окружения:**

Создайте файл `.env` в корне проекта:
```env
# Telegram Bot
BOT_TOKEN=your_telegram_bot_token
ADMIN_CHAT_ID=your_admin_chat_id

# Database
DB_PATH=sqlite+aiosqlite:///./abricol.db

# DeepSeek API
DEEPSEEK_API_KEY=your_deepseek_api_key
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_BASE_URL=https://api.deepseek.com

# Speech-to-Text (опционально)
STT_MODEL_SIZE=medium
STT_DEVICE=cpu
STT_COMPUTE_TYPE=int8
STT_LANGUAGE=ru

# Excel export (опционально)
LEADS_EXCEL_PATH=./leads.xlsx

# Google Sheets (опционально)
GOOGLE_SHEETS_CREDENTIALS_PATH=./path/to/credentials.json
GOOGLE_SHEETS_SPREADSHEET_ID=your_spreadsheet_id
```

5. **Постройте базу знаний:**
```bash
python -m src.build_kb
```

6. **Запустите бота:**
```bash
python -m src.bot
```

### 🐳 Запуск с Docker (рекомендуется для продакшн)

Проект полностью настроен для контейнеризации с использованием Docker, что обеспечивает быстрое и простое развертывание бота на любом сервере.

**Преимущества Docker-развертывания:**
- ✅ Изолированное окружение без конфликтов зависимостей
- ✅ Быстрое развертывание на любом сервере
- ✅ Автоматический перезапуск при сбоях
- ✅ Легкое масштабирование и обновление
- ✅ Единообразная работа на разных платформах

#### Быстрый старт с Docker Compose

1. **Создайте файл `.env`** (см. раздел "Настройка переменных окружения" выше)

2. **Запустите контейнер:**
```bash
docker-compose up -d
```

3. **Просмотр логов:**
```bash
docker-compose logs -f
```

#### Ручная сборка и запуск

1. **Соберите образ:**
```bash
docker build -t abricol-assistant .
```

2. **Запустите контейнер:**
```bash
docker run -d \
  --name abricol-assistant \
  --restart unless-stopped \
  --env-file .env \
  -v $(pwd)/abricol.db:/app/abricol.db \
  -v $(pwd)/knowledge.db:/app/knowledge.db \
  -v $(pwd)/leads.xlsx:/app/leads.xlsx \
  -v $(pwd)/src/knowledge/data:/app/src/knowledge/data \
  abricol-assistant
```

#### Управление контейнером

```bash
# Остановка
docker-compose stop

# Перезапуск
docker-compose restart

# Остановка и удаление
docker-compose down

# Пересборка образа
docker-compose build --no-cache
```

**Подробная документация по Docker:** см. файл [DOCKER.md](DOCKER.md)

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | Описание | Обязательная |
|-----------|----------|--------------|
| `BOT_TOKEN` | Токен Telegram бота | ✅ Да |
| `DEEPSEEK_API_KEY` | API ключ DeepSeek | ✅ Да |
| `ADMIN_CHAT_ID` | ID администратора | ❌ Нет |
| `DB_PATH` | Путь к БД SQLite | ❌ Нет |
| `STT_MODEL_SIZE` | Размер модели Whisper | ❌ Нет |
| `LEADS_EXCEL_PATH` | Путь к Excel файлу | ❌ Нет |

### Настройка базы знаний

PDF документы должны быть размещены в `src/knowledge/data/`. После добавления новых документов запустите:

```bash
python -m src.build_kb
```

Это создаст:
- Структурированные текстовые файлы в `src/knowledge/data/structured/`
- Изображения в `src/knowledge/data/images/`
- Индекс поиска в `knowledge.db`

## 📊 Модели данных

### UserProfile
Профиль пользователя с полной информацией:
- Статус (Обучение/Консультация/Читатель)
- Контактные данные (имя, телефон)
- Данные анкеты (опыт, уровень, цели)
- Согласие с политикой

### ChatMessage
История всех сообщений пользователя и бота

### ContactJournal
Журнал обращений для лидов со статусом "Обучение" или "Консультация"

### Lead
Записи о лидах (устаревшая модель, используется для совместимости)

## 🔧 Разработка

### Структура обработчиков

Бот использует FSM (Finite State Machine) для управления состояниями диалога:

- **Фаза 1**: Выбор статуса
- **Фаза 2**: Политика конфиденциальности
- **Фаза 3**: Анкетирование (только для "Обучение")
- **Фаза 4**: Запись контактов

### Добавление новых функций

1. Создайте новый обработчик в `src/handlers/`
2. Зарегистрируйте его в `src/bot.py`
3. При необходимости добавьте новые модели в `src/db/models.py`

### Тестирование

Для тестирования используйте тестовый бот от [@BotFather](https://t.me/BotFather).

## 📝 Форматирование Excel

При экспорте в Excel применяется следующее форматирование:
- Размер шрифта: 12
- Выравнивание: по центру (горизонтально и вертикально)
- Границы вокруг всех ячеек
- Перенос текста

## 🔒 Безопасность

- Все персональные данные защищены политикой конфиденциальности
- Согласие пользователя требуется перед обработкой данных
- Данные хранятся локально в SQLite
- Excel файлы могут быть заблокированы при открытии в Excel (автоматическое сохранение во временный файл)

## 💡 Примеры использования

### Взаимодействие с ботом

1. **Запуск бота:**
   - Отправьте команду `/start` в Telegram
   - Бот приветствует вас и показывает главное меню

2. **Задать вопрос:**
   - Напишите текстовое сообщение или отправьте голосовое
   - Бот найдет ответ в базе знаний и сгенерирует ответ с помощью AI

3. **Записаться на обучение:**
   - Нажмите кнопку "📝 Записаться"
   - Выберите "Обучение" или "Консультация"
   - Пройдите процесс записи

4. **Поиск по правилам:**
   - Используйте фразы типа "*правила корона*" для точного поиска
   - Бот покажет релевантную информацию и ссылку на первоисточник

## 🐛 Известные проблемы

- При открытом файле `leads.xlsx` в Excel может возникнуть ошибка доступа. Бот автоматически сохраняет данные во временный файл `leads.xlsx.tmp`
- Голосовые сообщения требуют больше времени на обработку из-за распознавания речи

## 🔄 Обновления

### Версия 1.0
- Базовая функциональность бота
- Интеграция с DeepSeek API
- Система записи на обучение
- Экспорт в Excel и Google Sheets
- Поддержка голосового ввода

## 👥 Авторы

Юрий Алинт

## 🤝 Вклад в проект

Приветствуются любые улучшения и предложения! Создавайте Issues и Pull Requests.

## 📞 Контакты

interual@gmail.com

---

**Примечание:** Для работы бота требуется активный интернет-соединение для доступа к Telegram API и DeepSeek API.

